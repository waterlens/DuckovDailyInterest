# DuckovT 语言手册

DuckovT 是一种用于在 Duckov Interest 应用中进行触发器求值的简单表达式语言。

## 数据类型

DuckovT 中只有一种核心数据类型，我们称之为 `值`。一个 `值` 内部表示为一个双精度浮点数。

### 数值

任何数字字面量（如 `123`、`4.56`）都是一个 `值`。

### 布尔值

`值` 可以被解释为布尔值。

*   `0` 等价于 `false`。
*   任何非零数字等价于 `true`。
*   您也可以直接使用 `true` 和 `false` 关键字。`true` 等价于 `1`，`false` 等价于 `0`。

## 变量

您可以在表达式中使用预定义的变量。变量名由字母、数字和下划线组成，但不能以数字开头。

例如，如果定义了一个名为 `balance` 的变量，您可以这样使用它：

```
balance * 0.05
```

## 运算符

DuckovT 支持多种运算符，并遵循特定的优先级和结合性规则。

### 算术运算符

| 运算符 | 描述 | 示例 |
| --- | --- | --- |
| `+` | 加法 | `a + b` |
| `-` | 减法 | `a - b` |
| `*` | 乘法 | `a * b` |
| `/` | 除法 | `a / b` |
| `%` | 取模 | `a % b` |
| `**` | 幂 | `a ** b` (右结合) |

### 比较运算符

| 运算符 | 描述 | 示例 |
| --- | --- | --- |
| `==` | 等于 | `a == b` |
| `!=` | 不等于 | `a != b` |
| `<` | 小于 | `a < b` |
| `>` | 大于 | `a > b` |
| `<=` | 小于或等于 | `a <= b` |
| `>=` | 大于或等于 | `a >= b` |

### 逻辑运算符

逻辑运算符作用于值的布尔解释。

| 运算符 | 描述 | 示例 |
| --- | --- | --- |
| `&&` | 逻辑与 | `a && b` |
| `||` | 逻辑或 | `a || b` |

### 一元运算符

| 运算符 | 描述 | 示例 |
| --- | --- | --- |
| `-` | 负号 | `-a` |

### 序列运算符

| 运算符 | 描述 | 示例 |
| --- | --- | --- |
| `;` | 顺序执行 | `e1; e2` |

`;` 运算符首先执行表达式 `e1`，然后执行表达式 `e2`，并返回 `e2` 的结果。

### 分组

您可以使用括号 `()` 来改变运算顺序。

```
(a + b) * c
```

## 内置函数

DuckovT 提供了一些内置函数，可以通过 `:` 运算符调用。

语法为 `值:函数名`。

| 函数名 | 描述 | 示例 |
| --- | --- | --- |
| `floor` | 向下取整 | `3.7:floor` (结果为 3) |
| `ceiling` | 向上取整 | `3.2:ceiling` (结果为 4) |
| `truncate` | 截断小数部分 | `3.7:truncate` (结果为 3) |
| `abs` | 绝对值 | `-5:abs` (结果为 5) |
| `sign` | 获取符号 | `-10:sign` (结果为 1), `10:sign` (结果为 1), `0:sign` (结果为 0) |
| `print` | 打印值到日志 | `123:print` (打印 123 并返回 123) |

`print` 函数会将其接收到的值打印到日志中，然后返回该值，以便进行链式调用。

## 运算符优先级

从低到高：

1.  `;`
2.  `||`
3.  `&&`
4.  `==`, `!=`, `<`, `>`, `<=`, `>=`
5.  `+`, `-`
6.  `*`, `/`, `%`
7.  一元的 `+`, `-`
8.  `**` (幂)
9.  `:` (函数调用)

## 示例表达式

*   计算利息：
    ```
    principal * (rate / 100)
    ```
*   检查条件：
    ```
    balance > 1000 && days_since_last_activity < 30
    ```
*   使用函数：
    ```
    (initial_value + adjustment):floor
    ```
*   使用序列和打印：
    ```
    (balance * 0.05):print; balance + (balance * 0.05)
    ```
    这个表达式会先计算 `balance * 0.05` 的结果并打印出来，然后计算 `balance + (balance * 0.05)` 并返回最终结果。